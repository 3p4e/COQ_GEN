[project]
name = "planner-gateway"
version = "0.1.0"
description = "Planner Letta gateway: the single allow-listed path from planner_api to the Letta agent fleet."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["planner_gateway*"]

[tool.ruff]
line-length = 100
