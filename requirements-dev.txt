# Dev/CI dependencies for the Python services (core_api + gateway + migrations).
# Pinned loosely for Phase 0; lock with a proper resolver (uv/pip-tools) later.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0.30
psycopg[binary]>=3.2
alembic>=1.13
httpx>=0.27
python-multipart>=0.0.9
sqlparse>=0.5
pytest>=8.2
pytest-asyncio>=0.23
ruff>=0.5
